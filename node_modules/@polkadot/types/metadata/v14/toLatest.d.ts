import type { CodecRegistry } from '@polkadot/types-codec/types';
import type { MetadataLatest, MetadataV14 } from '../../interfaces/metadata';
/**
 * Convert the Metadata (which is an alias) to latest
 * @internal
 **/
export declare function toLatest(registry: CodecRegistry, v14: MetadataV14, _metaVersion: number): MetadataLatest;
